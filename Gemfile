source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in googlebooksclient.gemspec
gemspec

group :test do
  gem 'rspec'
  gem 'webmock'
end

group :development do
  gem 'byebug'
end
